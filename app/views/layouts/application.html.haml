!!!
%html
  %head
    = stylesheet_link_tag 'compiled/screen.css', :media => 'screen, projection'
    = stylesheet_link_tag 'compiled/print.css', :media => 'print'
    /[if lt IE 8]
      = stylesheet_link_tag 'compiled/ie.css', :media => 'screen, projection'
    %title #{@page_title} - (nano)loop store
  %body.bp
    #container
      #header
        %h1 (nano)loop store
        %ul.menu
          %li= link_to "Home", root_path
          -if signed_in?
            %li= link_to "Your loops", loops_path

      #content
        #flash
          - flash.each do |key, value|
            %div{:id => "flash_#{key}"}
              &= value

        = yield
      #sidebar
        = yield :sidebar
      #footer
        &copy; jan krutisch / mindmatters GmbH & Co. KG
        |
        = link_to "Imprint / Impressum", imprint_path
        |
        - if signed_in?
          =link_to "Sign Out", sign_out_path, :method => :delete
        - else
          %span= link_to "Sign Up", sign_up_path
          %span= link_to "Login", sign_in_path

